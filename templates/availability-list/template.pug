//- Keep this line for correctly building PDFs
style(type="text/css") !{compiledStyle}

table.red
  thead
    tr
      th.header Code
      th Name
      th Price
  tbody
    each item in items
      tr
        td #{item.code}
        td #{item.name}
        td #{item.price}
